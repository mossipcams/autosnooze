name: Build

on:
  push:
    branches: [main, dev]
    paths:
      - '.github/workflows/**'
      - 'src/**'
      - 'tests/**'
      - 'custom_components/**'
      - 'package.json'
      - 'package-lock.json'
      - 'rollup.config.mjs'
      - 'requirements_test.txt'
      - 'pyproject.toml'
      - '.eslintrc.json'
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/**'
      - 'src/**'
      - 'tests/**'
      - 'custom_components/**'
      - 'package.json'
      - 'package-lock.json'
      - 'rollup.config.mjs'
      - 'requirements_test.txt'
      - 'pyproject.toml'
      - '.eslintrc.json'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # === Linting ===
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: 'requirements_test.txt'

      - name: Install Python dependencies
        run: pip install -r requirements_test.txt

      - name: Run ruff (Python linter)
        run: ruff check custom_components/ tests/ --output-format=github

      - name: Check ruff formatting
        run: ruff format --check --diff custom_components/ tests/

      - name: Run Pyright (Python type checker)
        run: pyright custom_components/autosnooze/

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install npm dependencies
        run: npm ci

      - name: Audit npm dependencies
        run: npm audit --audit-level=high

      - name: Run ESLint (JavaScript linter)
        run: npm run lint

      # === Validation ===
      - name: Validate version consistency
        run: |
          echo "Validating version consistency across files..."

          # Extract versions from all three sources
          PACKAGE_VERSION=$(jq -r '.version' package.json)
          MANIFEST_VERSION=$(jq -r '.version' custom_components/autosnooze/manifest.json)
          CARD_VERSION=$(grep -oP 'const CARD_VERSION = "\K[^"]+' src/autosnooze-card.js)

          echo "package.json version:       $PACKAGE_VERSION"
          echo "manifest.json version:      $MANIFEST_VERSION"
          echo "CARD_VERSION (source) version: $CARD_VERSION"

          # Validate all three versions match
          if [ "$PACKAGE_VERSION" != "$MANIFEST_VERSION" ] || [ "$PACKAGE_VERSION" != "$CARD_VERSION" ]; then
            echo ""
            echo "ERROR: Version mismatch detected!"
            echo "  package.json:             $PACKAGE_VERSION"
            echo "  manifest.json:            $MANIFEST_VERSION"
            echo "  CARD_VERSION (source):    $CARD_VERSION"
            echo ""
            echo "Please ensure all three files have the same version number."
            exit 1
          fi

          # Validate semantic versioning format (major.minor.patch)
          if ! echo "$PACKAGE_VERSION" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+$'; then
            echo "ERROR: Version '$PACKAGE_VERSION' does not follow semantic versioning format!"
            echo "Expected format: MAJOR.MINOR.PATCH (e.g., 1.0.0, 2.9.11)"
            exit 1
          fi

          echo ""
          echo "Version validation passed: $PACKAGE_VERSION"

      # === Testing ===
      - name: Run JavaScript tests with coverage
        run: npm run test:coverage
        # Vitest coverage thresholds are enforced via vitest.config.js (85% minimum)

      - name: Run Python tests with coverage
        run: pytest tests/ -v --cov=custom_components/autosnooze --cov-report=term-missing --cov-report=xml
        env:
          TZ: UTC
        # Python coverage threshold enforced via pyproject.toml (85% minimum)

      # === Build ===
      - name: Build card
        run: npm run build

      - name: Verify no bare imports in built file
        run: |
          if grep -E "from ['\"]lit['\"]" custom_components/autosnooze/www/autosnooze-card.js; then
            echo "ERROR: Built file contains bare 'lit' import - bundling failed!"
            exit 1
          fi
          echo "Build verified: no bare imports found"

      # === Artifacts ===
      - name: Upload built artifact
        uses: actions/upload-artifact@v4
        with:
          name: autosnooze-card
          path: custom_components/autosnooze/www/autosnooze-card.js
          retention-days: 7

      - name: Upload Python coverage report
        uses: actions/upload-artifact@v4
        with:
          name: python-coverage
          path: coverage.xml
          retention-days: 7

      - name: Upload JavaScript coverage report
        uses: actions/upload-artifact@v4
        with:
          name: js-coverage
          path: coverage/
          retention-days: 7
