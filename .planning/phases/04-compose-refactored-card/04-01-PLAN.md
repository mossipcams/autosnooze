---
phase: 04-compose-refactored-card
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/autosnooze-card.ts
  - src/styles/card.styles.ts
  - tests/test_card_ui.spec.js
  - tests/test_mutation_operators.spec.js
  - tests/test_mutation_coverage.spec.js
  - tests/test_backend_schema.spec.js
  - tests/test_boundary_mutations.spec.js
autonomous: true

must_haves:
  truths:
    - "The parent card is a thin orchestrator -- no large inline render blocks for pauses, duration, or filter/list"
    - "All three extracted child components are imported and rendered by the main card"
    - "Every existing test passes (npm run test:coverage reports all green)"
    - "Coverage meets 85% threshold on branches, functions, lines, and statements"
    - "The build succeeds and the bundle contains all custom element registrations"
    - "Python tests pass with 85%+ coverage"
  artifacts:
    - path: "src/components/autosnooze-card.ts"
      provides: "Thin orchestrator card (~600-760 lines, down from 1044+)"
      contains: "autosnooze-automation-list"
    - path: "src/index.ts"
      provides: "Entry point registering all 5 custom elements"
      contains: "autosnooze-automation-list"
    - path: "src/components/index.ts"
      provides: "Barrel export for all 5 component classes"
      contains: "AutoSnoozeAutomationList"
    - path: "src/styles/index.ts"
      provides: "Barrel export for all 5 style objects"
      contains: "automationListStyles"
  key_links:
    - from: "src/components/autosnooze-card.ts"
      to: "autosnooze-active-pauses, autosnooze-duration-selector, autosnooze-automation-list"
      via: "Lit template tags in render()"
      pattern: "<autosnooze-(active-pauses|duration-selector|automation-list)"
    - from: "src/index.ts"
      to: "customElements.define"
      via: "5 custom element registrations in dependency order"
      pattern: "customElements\\.define\\('autosnooze-"
    - from: "tests/*.spec.js"
      to: "src/utils/index.js"
      via: "Direct imports for utility functions (not pass-through methods)"
      pattern: "import.*from.*utils"
---

<objective>
Validate and clean up the refactored card after all three component extractions (Phases 1-3).

Purpose: Phase 4 is NOT a creation phase. By this point, all three child components (active-pauses, duration-selector, automation-list) have been extracted. This plan audits the parent card for dead code, removes pass-through utility methods that exist solely for test backward compatibility, verifies the full structural integrity of the refactored architecture, and confirms all tests pass with coverage thresholds met.

Output: A clean, thin orchestrator card with no dead code, all tests green, 85%+ coverage, and a successful build.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compose-refactored-card/04-RESEARCH.md

# Phase 3 summaries (needed -- Phase 3 produced the state we are validating)
@.planning/phases/03-extract-filter-list/03-01-SUMMARY.md
@.planning/phases/03-extract-filter-list/03-02-SUMMARY.md

# Key source files to audit
@src/components/autosnooze-card.ts
@src/components/index.ts
@src/styles/index.ts
@src/styles/card.styles.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit parent card and remove dead code</name>
  <files>
    src/components/autosnooze-card.ts
    src/styles/card.styles.ts
    tests/test_card_ui.spec.js
    tests/test_mutation_operators.spec.js
    tests/test_mutation_coverage.spec.js
    tests/test_backend_schema.spec.js
    tests/test_boundary_mutations.spec.js
  </files>
  <action>
    This task audits the post-Phase-3 parent card and removes dead code. Work through these steps in order:

    **Step 1: Baseline check**
    Run `npm run test:coverage` in /Users/matt/Desktop/Projects/autosnooze to establish the current coverage baseline. Record the per-file coverage for `autosnooze-card.ts`. If coverage is already below 85% on any metric, note which methods/lines are uncovered -- this informs whether pass-throughs can be safely removed.

    Also run `npx tsc --noEmit` and `npx eslint src/` to check for any pre-existing TypeScript or lint errors left over from Phase 3. Fix any errors found before proceeding.

    **Step 2: Remove 5 pass-through utility methods from parent card**
    These methods in `autosnooze-card.ts` are thin wrappers that exist ONLY for test backward compatibility. Remove them:

    1. `_parseDurationInput(input)` -- delegates to `parseDurationInput()` from utils
    2. `_formatDuration(d, h, m)` -- delegates to `formatDuration()` from utils
    3. `_combineDateTime(date, time)` -- delegates to `combineDateTime()` from utils
    4. `_getErrorMessage(error, default)` -- delegates to `getErrorMessage()` from utils
    5. `_formatRegistryId(id)` -- delegates to `formatRegistryId()` from state

    After removing these methods, also remove any imports that become unused (e.g., if `parseDurationInput` was imported solely for the pass-through, remove that import line). Run `npx eslint src/components/autosnooze-card.ts` after each removal to catch unused imports immediately.

    IMPORTANT: Before removing, check if any of these methods are called internally by other methods in the card (not just by tests). If a method IS used internally (e.g., `_snooze()` calls `_parseDurationInput()`), do NOT remove it -- only remove methods that are pure test-only pass-throughs.

    **Step 3: Update tests to import utility functions directly**
    For each removed pass-through method, update the test files that called it. The pattern:

    Before: `card._parseDurationInput('30m')`
    After: `import { parseDurationInput } from '../src/utils/index.js';` then `parseDurationInput('30m')`

    Before: `card._formatRegistryId('some_id')`
    After: `import { formatRegistryId } from '../src/state/automations.js';` then `formatRegistryId('some_id')`

    Affected test files and approximate assertion counts (from research):
    - test_card_ui.spec.js: ~21 assertions across _parseDurationInput (6), _formatDuration (5), _combineDateTime (5), _getErrorMessage (5)
    - test_mutation_operators.spec.js: ~18 assertions across _parseDurationInput (9), _formatDuration (7), _formatRegistryId (2)
    - test_mutation_coverage.spec.js: ~28 assertions across _parseDurationInput (8), _formatDuration (6), _combineDateTime (5), _getErrorMessage (5), _formatRegistryId (4)
    - test_backend_schema.spec.js: ~10 assertions for _getErrorMessage
    - test_boundary_mutations.spec.js: ~2 assertions for _formatRegistryId

    For each test file: add the direct import at the top, then find-and-replace `card._methodName(` with `methodName(`. The test logic and assertions stay identical -- only the call site changes.

    **Step 4: Audit CSS in card.styles.ts**
    After Phase 3, verify that `card.styles.ts` contains ONLY selectors used by:
    - The parent `render()` method
    - The `_renderScheduledPauses()` method
    - The `_showToast()` method (programmatic DOM creation)

    Selectors that MUST remain (do not remove):
    - `:host`, `ha-card` -- host styling
    - `.header`, `.header ha-icon`, `.status-summary` -- header
    - `.snooze-setup` -- child component wrapper
    - `.snooze-btn` and variants -- snooze button
    - `.list-header`, `.list-header ha-icon` -- used by `_renderScheduledPauses`
    - `.paused-info`, `.paused-name`, `.paused-time` -- used by `_renderScheduledPauses`
    - `.scheduled-list`, `.scheduled-item`, `.scheduled-icon`, `.scheduled-time`, `.cancel-scheduled-btn` -- scheduled section
    - `.toast`, `.toast-undo-btn`, `@keyframes slideUp` -- toasts
    - All corresponding `@media (max-width: 480px)` mobile overrides for the above

    Selectors that should have been removed by Phase 3 (verify they are gone):
    - `.filter-tabs`, `.tab`, `.tab-count` -- should be in automation-list.styles.ts now
    - `.search-box` -- should be in automation-list.styles.ts now
    - `.selection-list`, `.list-item`, `.list-item-*` -- should be in automation-list.styles.ts now
    - `.group-header`, `.group-badge` -- should be in automation-list.styles.ts now
    - `.selection-actions`, `.select-all-btn` -- should be in automation-list.styles.ts now
    - `.list-empty` -- should be in automation-list.styles.ts now

    If any of the "should be removed" selectors are still in `card.styles.ts`, remove them. Check for an `.empty` selector -- if `class="empty"` does not appear anywhere in `autosnooze-card.ts` templates, remove `.empty` from `card.styles.ts`.

    **Step 5: Verify removal did not break coverage**
    Run `npm run test:coverage` again. Compare per-file coverage for `autosnooze-card.ts` to the baseline.

    - If coverage DROPPED below 85% on any metric because of the pass-through removal: the utility function coverage is now attributed to utils files instead of the card. This is expected and acceptable as long as GLOBAL coverage is still 85%+. Check global thresholds.
    - If GLOBAL coverage dropped below 85%: revert the pass-through removal for the specific methods causing the drop. Keep those methods and their tests. Coverage threshold is the hard constraint.

    CRITICAL FALLBACK: If removing pass-throughs causes global coverage to drop below 85%, restore them. The research explicitly says: "Keep pass-throughs if coverage is already met after Phase 3, leave them. They are harmless."
  </action>
  <verify>
    1. `npx eslint src/components/autosnooze-card.ts` -- zero errors (no unused imports)
    2. `npx tsc --noEmit` -- zero errors
    3. `npm run test:coverage` -- all tests pass AND global 85% thresholds met
    4. Grep `card.styles.ts` for `.filter-tabs`, `.search-box`, `.selection-list`, `.group-header` -- zero matches (Phase 3 selectors removed)
    5. Grep `autosnooze-card.ts` for the 5 removed method names -- zero matches (or present only if fallback was needed)
  </verify>
  <done>
    Pass-through utility methods removed from parent card (or retained with documented reason if coverage required it). All affected tests updated to import utils directly. No dead CSS in card.styles.ts. TypeScript compiles clean, ESLint passes, all tests pass with 85%+ global coverage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Structural verification and full pipeline validation</name>
  <files>
    src/index.ts
    src/components/index.ts
    src/styles/index.ts
  </files>
  <action>
    This task verifies the complete structural integrity of the refactored architecture and runs the full validation pipeline.

    **Step 1: Verify component registration in src/index.ts**
    Confirm that `src/index.ts` registers all 5 custom elements in the correct dependency order (children before parent):
    1. `autosnooze-card-editor`
    2. `autosnooze-active-pauses`
    3. `autosnooze-duration-selector`
    4. `autosnooze-automation-list` (added by Phase 3)
    5. `autosnooze-card` (parent -- must be LAST)

    If `autosnooze-automation-list` is missing from `src/index.ts`, this means Phase 3 did not complete properly. Do NOT add it -- flag this as a Phase 3 gap.

    **Step 2: Verify barrel exports**
    Check `src/components/index.ts` exports all 5 component classes:
    - `AutomationPauseCard`
    - `AutomationPauseCardEditor`
    - `AutoSnoozeActivePauses`
    - `AutoSnoozeDurationSelector`
    - `AutoSnoozeAutomationList`

    Check `src/styles/index.ts` exports all 5 style objects:
    - `cardStyles`
    - `editorStyles`
    - `activePausesStyles`
    - `durationSelectorStyles`
    - `automationListStyles`

    If any are missing, add the missing export line.

    **Step 3: Verify parent render() composes all 3 child components**
    Read `src/components/autosnooze-card.ts` and confirm the `render()` method contains:
    - `<autosnooze-automation-list` tag
    - `<autosnooze-duration-selector` tag
    - `<autosnooze-active-pauses` tag

    The only remaining inline render helper should be `_renderScheduledPauses` (~30 lines). Verify there are no other large inline render blocks (>20 lines of HTML template) that should have been extracted.

    **Step 4: Count parent card lines**
    Run `wc -l src/components/autosnooze-card.ts` and record the result. The target is roughly 600-760 lines (down from 1044 pre-Phase 3, ~1300+ originally). This is a soft target -- the important thing is that the card is clearly an orchestrator with no large UI render blocks.

    **Step 5: Run full validation pipeline**
    Execute each of these commands and confirm they pass:

    1. `npm run test:coverage` -- All JS tests pass, 85%+ global coverage on all 4 metrics
    2. `npm run build` -- Rollup build succeeds
    3. Verify the built bundle at `custom_components/autosnooze/www/autosnooze-card.js`:
       - Contains all 5 `customElements.define` calls (grep for `autosnooze-card`, `autosnooze-active-pauses`, `autosnooze-duration-selector`, `autosnooze-automation-list`, `autosnooze-card-editor`)
       - No bare `lit` imports (grep for `from "lit"` or `from 'lit'` -- should find none, only `lit/` subpath imports)
    4. `pytest tests/ --cov=custom_components/autosnooze --cov-fail-under=85` -- All Python tests pass with 85%+ coverage
    5. `npx tsc --noEmit` -- Zero TypeScript errors
    6. `npx eslint src/` -- Zero lint errors

    If any step fails, diagnose and fix the issue. Common failure modes:
    - Build fails: Usually an import error from a removed method/file. Check rollup output for the specific error.
    - Python test failure: Should not happen (no Python changes). If it does, investigate -- it is a pre-existing issue.
    - Bundle missing element: Phase 3 may not have added the automation-list registration. Fix in src/index.ts.
  </action>
  <verify>
    1. `npm run test:coverage` exits 0 (all tests pass, coverage thresholds met)
    2. `npm run build` exits 0
    3. `grep -c "autosnooze-" custom_components/autosnooze/www/autosnooze-card.js` returns matches for all 5 elements
    4. `pytest tests/ --cov=custom_components/autosnooze --cov-fail-under=85` exits 0
    5. `npx tsc --noEmit` exits 0
    6. `npx eslint src/` exits 0
    7. `wc -l src/components/autosnooze-card.ts` shows a number significantly below 1044
  </verify>
  <done>
    All 5 custom elements registered in correct order. All barrel exports present. Parent render() composes all 3 child components with no large inline blocks remaining. Full validation pipeline green: tests pass, coverage 85%+, build succeeds, bundle valid, TypeScript clean, lint clean, Python tests pass. Parent card line count recorded for the summary.
  </done>
</task>

</tasks>

<verification>
Phase 4 is complete when ALL of the following are true:

1. **REF-01 (Main card broken into sub-components):** Parent card is a thin orchestrator composing 3 child components, with no large inline render blocks for pauses, duration, or filter/list. Line count significantly reduced from pre-refactoring baseline.

2. **REF-06 (All existing tests pass):** `npm run test:coverage` shows all tests passing. No test was deleted -- tests were either kept as-is or updated to import utility functions directly instead of calling pass-through methods.

3. **REF-07 (CI coverage thresholds met):** Both `npm run test:coverage` (85% JS) and `pytest --cov-fail-under=85` (85% Python) pass their thresholds.

4. **Build integrity:** `npm run build` succeeds, and the bundle contains registrations for all 5 custom elements with no bare `lit` imports.

5. **Code quality:** `npx tsc --noEmit` and `npx eslint src/` both report zero errors.
</verification>

<success_criteria>
- Parent card is clearly an orchestrator: renders 3 child component tags, contains no large inline template blocks for pauses/duration/filter-list
- Parent card is under ~760 lines (down from 1044 pre-Phase 3, ~1300+ originally)
- Dead pass-through methods removed (or retained with documented coverage justification)
- No dead CSS selectors in card.styles.ts
- All 5 custom elements registered in src/index.ts in dependency order
- All barrel exports present in components/index.ts and styles/index.ts
- npm run test:coverage passes with 85%+ on all 4 metrics
- npm run build succeeds with valid bundle
- pytest passes with 85%+ coverage
- TypeScript compiles clean, ESLint clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-compose-refactored-card/04-01-SUMMARY.md` using the summary template at @/Users/matt/.claude/get-shit-done/templates/summary.md.

Include in the summary:
- Final parent card line count (before and after Phase 4 cleanup)
- Which pass-through methods were removed vs retained (and why)
- Coverage numbers (per-file for card.ts and global)
- Any issues found and fixed during audit
- Confirmation that REF-01, REF-06, REF-07 are satisfied
</output>
